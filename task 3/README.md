# Task Automation with Python – Email Extractor

## 📌 Task Description
This project automates the task of extracting all email addresses from a text file and saving them into a separate output file.

## 🎯 Objective
To demonstrate task automation using Python by processing files and extracting useful information using regular expressions.

## 🛠️ Technologies Used
- Python
- os module
- re (Regular Expressions)
- File handling

## 📂 Files Description
- `email_extractor.py` → Python script to extract email addresses
- `input.txt` → Input file containing text with email addresses
- `emails.txt` → Output file containing extracted emails

## ▶️ How to Run the Program
1. Make sure Python is installed
2. Place `input.txt` in the same folder as the script
3. Run the script using:
   ```bash
   python email_extractor.py
